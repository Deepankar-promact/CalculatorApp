language: android
android:
    components:
        - tools
        - build-tools-25.1.3
        - android-25
        - extra-android-support
script:
    - ./gradlew build connectedCheck

before_install:
    - chmod +x ./gradlew
    
skip_cleanup: true
   